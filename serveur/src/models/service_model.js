class Service {
    constructor(servId, owner, title, description, pricePerHour) {
        this.servId = servId;
        this.owner = owner;
        this.title = title;
        this.description = description;
        this.pricePerHour = pricePerHour;
    }
}

module.exports = Service;
