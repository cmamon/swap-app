class Service {
    constructor(servId, title, description, pricePerHour) {
        this.servId = servId;
        this.title = title;
        this.description = description;
        this.pricePerHour = pricePerHour;
    }
}

module.exports = Service;
